# certificate-authority

This project is a certificate authority implementation in Golang.

## Installation

To install the certificate authority, you need to have golang installed. Then, you can clone the repository and build the project using the following commands:

```
go mod tidy
go mod vendor
go run main.go
```
